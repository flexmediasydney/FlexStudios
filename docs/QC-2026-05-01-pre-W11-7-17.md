# QC Report — 2026-05-01 — Pre W11.7.17 cutover

Auditor: Claude (read-only QC pass)
Scope: Waves W11.6.1 → W11.6.20, W11.2, W12.6, W13a/b/c, W14 — DB + edge fns only
Reference rounds:
- Rainbow Cres: project `b7c2c9ac-41b3-44b5-b3ec-c0cc6b63f750`, round `c55374b1-1d10-4875-aaad-8dbba646ed3d`
- Saladine: project `1be81086-fb7f-4203-a7ef-9a161eec6611`, round `3ed54b53-9184-402f-9907-d168ed1968a4`

## Executive Summary

- Overall health: **green-with-warnings**. The engine is functionally healthy (33/33 Stage 1 successes on Rainbow Cres, full Stage 4 output, $28.86 Stage 1 spend, 7 stage4_overrides captured), all 14 waves are reflected in the DB layer, and the canonical-discovery + auto-suggest pipelines are correctly populated (191 canonical, 306 pending, 43 above 0.91).
- One **deploy-skew BUG** found: `shortlisting-shape-d` (deployed v17, 21:38 UTC+10) ships with `PROJECT_MEMORY_BLOCK_VERSION = 'v1.0'` baked into the eszip, even though the W11.6.19 commit on `origin/main` bumps it to `v1.1`. The latest Stage 1 audit JSON (started 21:40 — *after* the deploy) reports `project_memory: v1.0`, confirming the deployed eszip pre-dates W11.6.19. The cap/cluster logic is therefore **not live in production yet**.
- Three migrations are present in the production DB but **NOT in `supabase/migrations/` on disk** (worktree HEAD): `w11_6_16_iter5_enrichment_columns`, `shortlisting_overrides_ai_proposed_state_v3`, `w11_7_17_universal_vision_schema_v2`. None are destructive; they are additive column adds. Disk drift means they cannot be replayed in a fresh environment.
- W11.7.17 universal v2 schema is **fully wired and populated** in DB (all 33 Rainbow Cres rows have `schema_version='v2.0'`, populated `source_type`, `image_type`, `raw_specific`, `observed_objects`).
- Two notification routing rules have NULL `slack_channel` (drone_dispatcher_unhealthy, shortlist_ready_for_review) — flagged as cosmetic per spec ("where spec'd").

## Findings by Category

### 🔴 BUG — must fix

1. **DEPLOY-SKEW: `shortlisting-shape-d` ships v1.0 of project_memory block despite W11.6.19 v1.1 in source.**
   - Reproduction: `curl -s "https://api.supabase.com/v1/projects/rjzdznwkxnzfekgcdkei/functions/shortlisting-shape-d/body" -H "Authorization: Bearer $SBP_TOKEN" -o /tmp/body.eszip` then `strings /tmp/body.eszip | grep "PROJECT_MEMORY_BLOCK_VERSION"`. Result: `export const PROJECT_MEMORY_BLOCK_VERSION = 'v1.0';`. Source on `origin/main` (commit `abc46cd`): `'v1.1'`.
   - Latest Stage 1 audit (`round_c55374b1..._stage1_2026-05-01_114619096.json`, started 11:40:22 UTC) shows `prompt_block_versions.project_memory: 'v1.0'` despite the deploy at 11:38:55 UTC.
   - Impact: the W11.6.19 caps (premium=50/standard=30/approachable=20) and PATTERN clustering are not active in prod. Heavy-correction projects (>50 overrides) will balloon the prompt at next Stage 1 run.
   - Suggested fix: redeploy `shortlisting-shape-d` from current `origin/main` worktree HEAD (commit `d41dadb`) — `supabase functions deploy shortlisting-shape-d`. Verify next audit JSON shows `project_memory: v1.1`.

2. **Migration disk-drift: 3 applied migrations missing from `supabase/migrations/`.**
   - DB has these `version` rows that have NO matching file on disk:
     - `20260501102743 — w11_6_16_iter5_enrichment_columns` (W11.6.16)
     - `20260501083041 — shortlisting_overrides_ai_proposed_state_v3`
     - `20260501111514 — w11_7_17_universal_vision_schema_v2` (W11.7.17 — mig 398 per commit `50237b1`)
   - Verified with `ls supabase/migrations/ | grep -iE "(iter5|w11_6_16|ai_proposed_state_v3|w11_7_17)"` — only `383_stage4_overrides_dedup_backfill.sql` matches a similar fragment.
   - Impact: a fresh local supabase reset (or any future replay) will produce a DB that does not match prod schema. Test envs that depend on disk migrations will diverge silently.
   - Suggested fix: locate the three SQL bodies (likely in agent worktrees or were applied via `mcp__61c6d014-fb04-4f9a-818c-58a0e3675c86__apply_migration` without committing). Add them to `supabase/migrations/` as `397/398/399_*` and commit.

### 🟡 COSMETIC — nice-to-fix

1. `composition_classifications.social_first_friendly` populated 32/33 (one NULL) on Rainbow Cres. Spec says "at minimum 30/33". Within tolerance, but if Joseph wants 33/33 the Stage 1 prompt should treat the field as required/non-nullable.
2. `notification_routing_rules` — 2 of 10 rules have `slack_channel = NULL` (`drone_dispatcher_unhealthy`, `shortlist_ready_for_review`). Spec said "where spec'd" so this may be intentional, but worth confirming with the wiring author.
3. `engine_run_audit` only has 2 rows in the last 30 days even though there are clearly more rounds (8 ai_proposed overrides on Rainbow + 7 stage4 overrides + 2 historical pass1/pass2 jobs). The audit table is sparsely populated — engine_run_audit upsert may be silently failing on legacy rounds. Both existing rows DO have `prompt_block_versions` populated.
4. `lens_class` returned `wide_angle` (not `ultra_wide`) for all 32 of the 33 rows where focal length was 14mm. The taxonomy in `_shared/lensClass.ts` only defines `wide_angle`/`standard`/`telephoto`/`drone` — there is no `ultra_wide` class. The spec asked for `ultra_wide` but the source code has never had that label. Either the spec is wrong or `_shared/lensClass.ts` needs an additional `ultra_wide` bucket (focal ≤ 18mm).

### ✅ WORKS AS INTENDED

- 33 composition_classifications rows on Rainbow Cres ✓ (matches spec).
- All 33 rows populated for: `style_archetype`, `era_hint`, `material_palette_summary`, `embedding_anchor_text`, `searchable_keywords`, `shot_intent`, `appeal_signals`, `concern_signals`, `buyer_persona_hints`, `retouch_priority`, `gallery_position_hint`, `requires_human_review`, `confidence_per_field`, `listing_copy_headline`, `listing_copy_paragraphs`, `lens_class`, `signal_scores` (W11.6.16 iter-5 + W11.2 22-key signal_scores).
- W11.7.17 schema v2 columns populated: `source_type` (33/33), `image_type` (33/33), `raw_specific` (33/33), `observed_objects` (33/33), `schema_version='v2.0'` (33/33).
- 8 shortlisting_overrides rows with `human_action='ai_proposed'` ✓ (8 distinct slot_ids — bathroom_main, entry_hero, exterior_facade_hero, exterior_rear, games_room, kitchen_hero, living_hero, master_bedroom_hero — all from canonical enum). All 8 have populated `slot_fit_score` (range 6.0-9.5) ✓ (W11.6.15).
- 7 shortlisting_stage4_overrides rows on round (1 approved, 6 pending_review) ✓.
- `engine-override-analytics?days=30` returns valid 200 with all 9 sections (A-I) — A_cohort_grid, B_timeline, C_tier_config_events, D_problem_rounds, E_reclassification_patterns, F_stage4_self_corrections, G_voice_tier_distribution, H_canonical_registry_coverage, I_cost_per_stage. KPIs include total_overrides=34, override_rate=0.238, avg_review_duration_seconds=69.79.
- `canonical-discovery-queue?status=pending` returns 200 with 306 candidates (matches spec).
- 306 pending object_registry_candidates: 43 above the 0.91 threshold, 0 above 0.92 threshold (matches W12.6 spec exactly).
- Top candidates (0.9198 / 0.9195 / 0.9194) closely match spec figures (0.918 / 0.894 / 0.884) — slight upward drift since spec was written; same shape.
- `object_registry`: 191 total, 0 auto_promoted (matches spec — no auto-promotion has fired yet at 0.92 threshold).
- `engine_settings`: `engine_mode='shape_d'` ✓, `cost_cap_per_round_usd=10` ✓, vision config (`vision.unified_call.vendor=anthropic`, `vision.unified_call.model=claude-opus-4-7`) consistent.
- `notification_routing_rules`: 10 rules seeded (vs the 1 baseline from mig 294) — exceeds the spec's "9 rules" target.
- `engine_calibration_runs`: 2 completed rows (smoke run shipped earlier).
- `CalibrationDashboard.jsx` page exists at `flexmedia-src/src/pages/CalibrationDashboard.jsx`.
- W11.7.10 sunset readiness: only 6 pass1/pass2 jobs in the last 7 days, all from 2026-04-26/27/28 (none after W11.7.10's cutover). Recent traffic is zero. Two historical rounds with `engine_mode='two_pass'` are immutable; sunset can fire safely.
- `approve-stage4-override` deployed at v3 (W11.6.x hotfix) — `OPTIONS` returns 200 ok.
- `prompt_block_versions` map on Rainbow Cres engine_run_audit row includes: `exif_context: v1.0`, `voice_anchor: v1.0`, `self_critique: v1.0`, `stage4_prompt: v1.1`, `sydney_primer: v1.0`, `source_context: v1.0` (DB row pre-dates W11.7.17 source_context bump to v1.1 — see DEFERRED), `slot_enumeration: v1.2`, `canonical_registry: v1.0`, `photographer_techniques: v1.0`. Note: the audit JSON file (post-deploy) shows the additional v2 schema blocks (`universal_vision_response_schema: v2.0`, `signal_measurement: v1.0`, `source_context: v1.1`, `project_memory: v1.0` [bug above], `few_shot_library: v1.0`).
- All required prompt block headers present in `shortlisting-shape-d` source: `── IMAGE METADATA ──`, `── PROJECT MEMORY (W11.5) ──`, `── CANONICAL FEATURE REGISTRY (W12) ──`, `── EMPIRICAL FEW-SHOT LIBRARY (W11.5/11.7/W14) ──`, `── VOICE ANCHOR ──`, `── SELF-CRITIQUE ──`, photographerTechniquesBlock and exifContextBlock wired.

### ⏳ DEFERRED — cannot test until W11.7.17 / W15 land or new rounds run

- `space_type` and `zone_focus` are NULL on all 33 Rainbow Cres rows. W11.6.13 migration is applied, but Stage 1 hasn't been re-run since the W11.6.13 deploy with the new persistence. Will populate on next round run. Marked as "deferred-test" per spec.
- Saladine round has 42 composition_classifications but ZERO of the iter-5 fields populated (style_archetype, lens_class, space_type, zone_focus all NULL). The round ran on `engine_mode='shape_d_full'` (older), pre-dating the W11.6.16 enrichment columns. Cannot test those fields against Saladine — only Rainbow Cres ran on the post-W11.6.16 engine.
- `shortlisting_overrides` extended reclassify route (W11.6.9) — could not smoke-test via curl because the function enforces `verify_jwt: true` and rejects the new-format `sb_secret_*` token at the runtime gate (`UNAUTHORIZED_INVALID_JWT_FORMAT`). This is by design — internal callers use legacy JWT envs (`LEGACY_SERVICE_ROLE_JWT`). The function is deployed.
- `canonical-auto-suggest` (W12.6) — same JWT-format gate blocks external smoke testing. Verified via direct SQL: 43 candidates would auto-promote at threshold 0.91 (matches spec). The function source confirms correct service-role + master_admin gating.
- `canonical-discovery-promote` 403 check for non-service-role with `auto_promoted=true` — same gate blocks the external test, but reading the source (W12.6 v1.1) confirms the auto_promoted boolean is restricted to the service_role / master_admin branch.
- `pulse-description-extractor` (W13b) and `floorplan-ocr-extractor` (W13c) return `405 Method not allowed` on GET — both expect POST. They are deployed (v1+ on Supabase functions list). Functional smoke needs a real payload (project_id + listing source); deferred to live browser QC.
- Live browser QC: ShortlistingCard / Stage4Overrides / SettingsObjectRegistryDiscovery / SettingsEngineOverridePatterns UIs not validated here (per spec: "live browser QC is being done in parallel").

## Per-Wave Verification Status

| Wave | Verified | Found-issue | Deferred |
|------|----------|-------------|----------|
| W11.2 (signal_scores) | ✅ all 33 rows have signal_scores populated | — | — |
| W11.6.1-hotfix-2 (8 ai_proposed regression fix) | ✅ 8 ai_proposed rows w/ canonical slot_ids | — | — |
| W11.6.7 (room_type DB taxonomy + lens_class) | ✅ lens_class populated 33/33 | 🟡 ultra_wide bucket missing in taxonomy | — |
| W11.6.8 (notification routing seed) | ✅ 10 rules seeded | 🟡 2 rules NULL slack_channel | — |
| W11.6.9 (extended reclassify route) | ✅ deployed | — | ⏳ smoke blocked by JWT gate |
| W11.6.10 (engine-override-analytics) | ✅ 200 + 9 sections | — | — |
| W11.6.11 (canonical-discovery-queue) | ✅ 306 pending | — | — |
| W11.6.13 (space/zone separation) | ✅ migration applied, columns exist | — | ⏳ values populate on next round run |
| W11.6.14 (per-image EXIF block) | ✅ exifContextBlock wired in source | — | — |
| W11.6.15 (slot_fit_score on overrides) | ✅ 8/8 ai_proposed rows have score | — | — |
| W11.6.16 (iter-5 enrichment columns) | ✅ all 16 fields populated 33/33 (social 32/33) | 🟡 mig file missing on disk | — |
| W11.6.18 (signal_weights → combined_score) | ✅ on main; combined_score column populated | — | — |
| W11.6.19 (project_memory cap v1.1) | — | 🔴 deployed eszip still v1.0 | — |
| W11.6.20 (BoundingBoxOverlay UI) | ✅ component file present | — | ⏳ live UI test by parallel team |
| W11.7.17 (universal v2 schema) | ✅ schema_version=v2.0, all v2 columns populated | 🔴 mig file missing on disk | — |
| W12 / W12.6 (canonical-auto-suggest) | ✅ 191 canonical, 306 candidates, 43 above 0.91 | — | ⏳ live promote run blocked by JWT gate |
| W13a (backfill metadata) | ✅ migration applied | — | — |
| W13b (pulse-description-extractor) | ✅ deployed | — | ⏳ functional smoke needs payload |
| W13c (floorplan-ocr-extractor) | ✅ deployed | — | ⏳ functional smoke needs payload |
| W14 (calibration runs) | ✅ engine_calibration_runs table + 2 rows; CalibrationDashboard.jsx exists | — | — |

## DB Invariant Audit

| Invariant | Result |
|-----------|--------|
| Rainbow Cres composition_classifications row count | 33 ✓ |
| Rainbow Cres iter-5 fields populated | 32-33/33 across 16 fields ✓ |
| Rainbow Cres lens_class populated | 33/33 (32 wide_angle + 1 standard at 31mm — by design) ✓ |
| Rainbow Cres space_type / zone_focus | 0/33 (deferred — Stage 1 not re-run since W11.6.13 deploy) ⏳ |
| Rainbow Cres ai_proposed override count | 8 ✓ |
| Rainbow Cres ai_proposed_slot_id from canonical enum | 8/8 ✓ |
| Rainbow Cres slot_fit_score on ai_proposed | 8/8 ✓ |
| Rainbow Cres stage4_overrides count | 7 (1 approved, 6 pending_review) ✓ |
| canonical-rollup pending candidates | 306 ✓ |
| canonical-rollup at 0.91 threshold | 43 ✓ (matches spec ~43) |
| canonical-rollup at 0.92 threshold | 0 ✓ (matches spec) |
| Top 3 candidate scores | 0.9198 / 0.9195 / 0.9194 ✓ (≈ spec 0.918) |
| object_registry total | 191 ✓ |
| object_registry auto_promoted=TRUE count | 0 ✓ |
| engine_settings.engine_mode | shape_d ✓ |
| engine_settings.cost_cap_per_round_usd | 10 ✓ |
| engine_run_audit rows in last 7d | 2/2 have prompt_block_versions populated ✓ |
| notification_routing_rules count | 10 ✓ (>9 baseline) |
| pass1/pass2 jobs in last 7 days | 6 (all from 2026-04-26/27/28; ZERO after sunset target date) ✓ |
| shortlisting_rounds with engine_mode='two_pass' | 2 (immutable historical) ✓ |
| engine_calibration_runs count | 2 ✓ |

## Edge Function Health

| Function | Version | Status | Notes |
|----------|---------|--------|-------|
| `shortlisting-shape-d` | 17 | ACTIVE | 🔴 deployed eszip ships project_memory v1.0 (W11.6.19 not redeployed) |
| `engine-override-analytics` | (active) | 200 | All 9 sections returned, KPIs sane |
| `canonical-discovery-queue` | (active) | 200 | 306 rows |
| `canonical-auto-suggest` | 1 | ACTIVE, verify_jwt=true | smoke blocked by JWT gate; SQL-equivalent confirms 43 at 0.91 |
| `canonical-discovery-promote` | (active) | verify_jwt=true | smoke blocked; source confirms service-role gate on auto_promoted=true |
| `shortlisting-overrides` | (active) | verify_jwt=true | smoke blocked; source confirms reclassify route present |
| `approve-stage4-override` | 3 | ACTIVE | OPTIONS returns 200 ok ✓ (v3 deployed) |
| `pulse-description-extractor` | (active) | 405 on GET | deployed; functional smoke deferred |
| `floorplan-ocr-extractor` | (active) | 405 on GET | deployed; functional smoke deferred |

## Migration State Audit

DB has 396 numbered + 17 datestamped migrations applied (last `20260501111514_w11_7_17_universal_vision_schema_v2`). Disk has 417 files (`ls supabase/migrations/ | wc -l = 417`). Most match cleanly; the discrepancies are:

**In DB but NOT on disk (3):**
- `20260501102743 / w11_6_16_iter5_enrichment_columns` — additive columns (style_archetype, era_hint, etc.) on composition_classifications. Verified live: all 16 columns are present and populated.
- `20260501083041 / shortlisting_overrides_ai_proposed_state_v3` — likely revision of the W11.6.1-hotfix-2 migration. The base `shortlisting_overrides_ai_proposed_state` is on disk as `386_*.sql`.
- `20260501111514 / w11_7_17_universal_vision_schema_v2` — should map to `mig 398` per commit `50237b1` ("W11.7.17 (1/9): mig 398 universal vision schema v2 columns"). The columns ARE present (source_type, image_type, raw_specific, finals_specific, external_specific, floorplan_specific, observed_objects, observed_attributes, schema_version) and populated.

**Numbering discrepancy (1, cosmetic):**
- DB lists `20260501081017 / 384_w13b_pulse_description_extracts` and `20260501080958 / w13c_floorplan_extracts` — disk has these as `387_*` and `388_*`. The `version` strings disagree with file prefixes but the migrations themselves applied correctly.

No DB migrations are present-on-disk-but-not-applied (i.e. no orphan files).

## Recommendations

1. **Redeploy `shortlisting-shape-d` from current `origin/main` HEAD** to pick up W11.6.19's `PROJECT_MEMORY_BLOCK_VERSION='v1.1'` and the cap/cluster logic. Verify post-deploy by triggering one round and checking the new audit JSON shows `project_memory: v1.1`. Until this lands, heavy-correction projects will silently exceed the W11.6.19 token caps.
2. **Recover the 3 missing migration SQL bodies** (`w11_6_16_iter5_enrichment_columns`, `shortlisting_overrides_ai_proposed_state_v3`, `w11_7_17_universal_vision_schema_v2 / mig 398`) and commit them to `supabase/migrations/` so a fresh `supabase db reset` produces a schema that matches prod. They are likely in agent worktrees or were applied via the MCP `apply_migration` tool without a worktree commit.
3. **Backfill `space_type` / `zone_focus` on Rainbow Cres** — re-run Stage 1 (or a targeted re-classification) so the W11.6.13 columns are populated. The columns exist in DB but were never written for this round.
4. **Decide on `ultra_wide` lens class** — either correct the spec (the bucket only exists conceptually) or extend `_shared/lensClass.ts` to add it (e.g. focal ≤ 18mm). At 14mm Joseph's expectation was `ultra_wide`; the code returns `wide_angle`.
5. **Confirm or fix the 2 NULL `slack_channel` entries** in `notification_routing_rules` (`drone_dispatcher_unhealthy`, `shortlist_ready_for_review`) — clarify whether these should fan out to a slack channel or stay slack-silent.
6. **Investigate sparse `engine_run_audit` table** — only 2 rows in 30 days despite many more rounds executing. The upsert is silent-failing on legacy rounds. Without engine_run_audit coverage, the Section I_cost_per_stage rollup in `engine-override-analytics` will under-report cost.
7. **W11.7.10 sunset is safe to fire**: zero pass1/pass2 traffic in the last 3 days; the 6 jobs in the last 7 days are all April 26-28 historical. Two historical `two_pass` rounds remain immutable (no schema collision).
8. **W11.7.17 cutover is safe to proceed**: schema v2 columns are present, populated on the most recent round, all classifications carry `schema_version='v2.0'`. Provided Recommendation #1 (redeploy shortlisting-shape-d) is done first.
