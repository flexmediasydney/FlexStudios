/**
 * DronePipelineBanner — Wave 9 Stream 2 (PLACEHOLDER SHIM)
 *
 * ⚠️ S3 SHIM: this file is a build-passing placeholder that renders nothing.
 * Stream 2 owns this file and will replace it with the real banner UI
 * (current stage / ETA / Force-fire / Re-run controls).
 *
 * Planned props (locked with S2):
 *   { pipelineState, onForceFire, onRerunStage, isFiring, isRerunning }
 */

export default function DronePipelineBanner(_props) {
  // No-op until S2 ships the real banner. Returning null keeps the page
  // layout intact and the test "page loads without crash" passing.
  return null;
}
